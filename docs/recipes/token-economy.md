# Recipe — Token Economy

Goal:
Show known and estimated token usage.

Commands:
```bash
pala token-economy
pala token-economy --json
```

Rules:
- exact and estimate separated
- fake exact cost forbidden

Expected evidence:
- known and estimated token fields
- confidence and source
- Unknown instead of fake zero when usage is unavailable
